import { Outlet } from '@tanstack/react-router';
import React from 'react';

const AdminLogin = () => {
  return (
    <div>
      AdminLogin
      <Outlet />
    </div>
  );
};

export default AdminLogin;
