import { Link, Outlet } from '@tanstack/react-router';
import React from 'react';

const About = () => {
  return (
    <div>
      <Link to="/Admin/Dashboard/About/aboutoptions">About</Link>
    </div>
  );
};

export default About;
